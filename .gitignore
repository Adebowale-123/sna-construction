# Node
node_modules/
npm-debug.log*

# Build outputs
frontend/dist/
backend/dist/

# Environment
.env
.env.local
.env.*.local

# Database (local only — Railway uses its own volume)
backend/prisma/*.db
backend/prisma/*.db-journal

# Uploads (local only)
backend/uploads/*
!backend/uploads/.gitkeep

# OS
.DS_Store
Thumbs.db
